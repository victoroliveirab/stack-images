{
  "name": "stack-images",
  "version": "0.1.0",
  "description": "A simple website to join multiple screenshots into one image for simple sharing",
  "main": "dist/index.js",
  "scripts": {
    "deploy": "npm run build && git add ./dist/app.js && git commit -m \"New app.js distribution\" && git push origin main",
    "build": "webpack --mode production",
    "dev": "python -m SimpleHTTPServer",
    "watch": "webpack --mode development --watch",
    "watch:prod": "webpack --mode production --watch"
  },
  "keywords": [
    "images",
    "clipboard",
    "canvas"
  ],
  "author": "Victor Oliveira",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.5.1",
    "ts-loader": "^9.2.6",
    "typescript": "^4.5.4",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1"
  }
}
